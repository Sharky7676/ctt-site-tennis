{
  "name": "sierre-tennis-cloud",
  "version": "1.0.0",
  "description": "Site de tennis de table de Sierre avec sauvegarde cloud",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build complete'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}

